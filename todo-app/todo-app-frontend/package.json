{
  "name": "todo-app-frontend",
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "build": "bun build ./src/index.tsx --target bun --minify --bytecode --sourcemap --metafile --metafile-md --compile --outfile server"
  },
  "dependencies": {
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}

{
  "name": "todo-app-backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build ./src/index.ts --target bun --minify --bytecode --sourcemap --metafile --metafile-md --compile --outfile server"
  },
  "dependencies": {
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}

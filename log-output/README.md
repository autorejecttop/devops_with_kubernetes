# Log Output App

Continuing from Exercise 1.1, I created the `manifests/deployment.yaml` file using the example shown on the course material, and then do:

```bash
kubectl apply -f manifests/deployment.yaml
```
